import React from 'react';

import classes from './Toolbar.module.less';


const toolbar = (props) => {

    return (
        <div className={classes.Toolbar}>
            <h1>NAME GENERATOR</h1>
        </div>
    );
    
}

export default toolbar;
